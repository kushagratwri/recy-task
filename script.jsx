function startCountdown(){const targetDate=new Date("2025-01-01T00:00:00").getTime();const timerElement=document.getElementById("timer");function updateTimer(){const now=new Date().getTime();const difference=targetDate-now;if(difference<=0){timerElement.innerHTML="Happy New Year!";clearInterval(interval);return;}const days=Math.floor(difference/(1000*60*60*24));const hours=Math.floor((difference%(1000*60*60*24))/(1000*60*60));const minutes=Math.floor((difference%(1000*60*60))/(1000*60));const seconds=Math.floor((difference%(1000*60))/1000);timerElement.innerHTML=`${days}d:${hours}h:${minutes}m:${seconds}s`;}const interval=setInterval(updateTimer,1000);updateTimer();}window.onload=startCountdown;
